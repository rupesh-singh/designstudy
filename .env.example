# LLM ranking is optional. Without it the pipeline uses heuristic scoring.
# Any OpenAI-compatible endpoint works (OpenAI, Azure, GitHub Models, Ollama).
DIGEST_LLM_API_KEY=
DIGEST_LLM_BASE_URL=https://api.openai.com/v1
DIGEST_LLM_MODEL=gpt-4o-mini
